language: go

addons:
  apt:
    packages:
      - docker-ce

services:
  - docker

script:
  - ./bin/docker_build.sh

deploy:
  provider: script
  script: ./bin/docker_push.sh
  on:
    branch: master

